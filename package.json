{
  "name": "query_parser",
  "version": "1.0.0",
  "description": "This folder contains query parser files. - Invoke the main function from serializer.js to parser the query and get the proto.  - Invoke the parse function from parser.js to generate the AST",
  "main": "index.js",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "test": "mocha tests --recursive"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "chai": "^4.3.6",
    "mocha": "^9.2.0"
  },
  "dependencies": {
    "lodash": "^4.17.21"
  }
}
